name: Update game journal

on:
  push:
    branches:
      - main
    paths:
      - "games/**"
      - "scripts/update_readme.py"
      - "scripts/build-site.py"
      - ".github/workflows/update-readme.yml"

  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-journal:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the journal
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install generator dependencies
         run: python -m pip install -r scripts/requirements.txt

      - name: Update the README
        run: python scripts/update_readme.py

      - name: Build searchable journal data
        run: python scripts/build-site.py

      - name: Show generated files
        run: |
          echo "Contents of docs:"
          ls -la docs
          echo "Generated game count:"
          python -c "import json; print(len(json.load(open('docs/games.json'))))"

      - name: Commit generated files
        run: |
          git config user.name "rp6-journal-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add README.md docs/games.json

          if git diff --cached --quiet; then
            echo "Everything is already up to date."
            exit 0
          fi

          git commit -m "Update game journal"
          git push
